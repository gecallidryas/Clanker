# Femmy Discord Bot Dependencies
# Install with: pip install -r requirements.txt

# Discord API wrapper (async)
discord.py==2.7.1

# Google Gemini AI SDK
google-genai>=0.6.0

# Async SQLite for database operations
aiosqlite>=0.19.0

# Encryption + password hashing
cryptography>=42.0.0
bcrypt>=4.1.0

# Environment variable management
python-dotenv>=1.0.0

# Timezone handling
pytz>=2024.1

# HTTP client for portfolio checks
aiohttp>=3.9.0

# Web search + extraction
duckduckgo-search>=6.0.0
trafilatura>=1.6.0

# YouTube processing
pytube>=15.0.0
youtube-transcript-api>=0.6.0

# System monitoring for stats command
psutil>=5.9.0

# Image processing for vision
Pillow>=10.0.0

# WebP encoder support for Pillow (system package on Ubuntu)
# Install: sudo apt-get install -y libwebp libwebp-dev

# OpenAI SDK (used for OpenRouter API compatibility)
openai>=1.0.0

# Postgres RAG + pgvector
asyncpg>=0.29.0
pgvector>=0.3.2

# PDF parsing for RAG documents
pypdf>=4.0.0
